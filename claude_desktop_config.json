{
  "mcpServers": {
    "quill-mcp": {
      "command": "python",
      "args": ["-m", "quill_mcp.server"],
      "env": {},
      "description": "Quill MCP - Local memory server for authors"
    }
  }
}